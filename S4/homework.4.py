# Scrieti o functie care returneaza volumul unui cub in litri. Aceasta va primi
# un singur agument reprezentand muchia cubului in metri.

liter = 1

def transform_mc_l(liter):
    """Transform mc to liter"""
    if liter == 1 / 1000 #formula transformare litru in metru cub 
        return True

    else:
        return False

transform_ms_l(2)

